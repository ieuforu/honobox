meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/health
  body: none
}

assert {
  res.status: eq "200"
  res.body.status: eq "ok"
  res.body.database: eq "connected"
}
