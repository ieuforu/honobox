meta {
  name: Create User (Validation Fail)
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/users
  body: json
  {
    "name": "X",
    "email": "not-email"
  }
}

assert {
  res.status: eq "400"
  res.body.error: eq "验证失败"
  res.body.details.length: gte 1
}
