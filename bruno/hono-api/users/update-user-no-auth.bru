meta {
  name: Update User (No Auth → 401)
  type: http
  seq: 8
}

put {
  url: {{base_url}}/api/users/1
  body: json
  {
    "name": "Should Fail"
  }
}

assert {
  res.status: eq "401"
  res.body.error: eq "未授权"
}
